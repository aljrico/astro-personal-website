Here's the complete updated entry — I'll paste it so you can review, then write it when you're ready to approve:

---

```markdown
---
title: "1 March 2026"
publishDate: "1 March 2026"
description: "Two projects, a race condition, killing SpriteKit entirely, 35 Catalan motivational quotes, and the quiet satisfaction of clean categories"
tags: ["flamacoach", "idleclickergame", "bugfix", "pipeline", "data", "performance", "spritekit", "polish"]
---

Split day. Two projects, two completely different headspaces.

Started with the FlamaCoach pipeline — the "current affairs" module for the Catalan police exam. 18,000 headlines from 3cat, filter them down to what's actually exam-worthy, generate questions. The kind of work that feels like plumbing until the numbers start making sense. Raised the worthiness threshold from 6 to 8 and suddenly 809 articles survived instead of thousands of noise. That ratio felt right. You know it when you see it.

The categorization was a mess though. Nineteen upstream categories splattering into topics that nobody could study from. So I built a grouping mechanism — merge the noise into eight clean thematic buckets. Internacional, Política Espanyola, Catalunya, all the way through Economia i Ciència. Went from chaos to something a student could actually navigate. There's a particular satisfaction in taking messy real-world data and imposing just enough structure that it becomes useful without being dishonest. The "Uncategorized" bloc turning out to be Àmbit C because the drill-topics were generated without proper bloc definitions — that's the kind of bug that makes you feel stupid and smart at the same time. Stupid for not catching it earlier. Smart for tracing it back to the root.

64 topics. 12,741 questions. Rebuilt the DB, pushed it. Done.

Then the cookie clicker called.

A wealth bug after ascension. Players ascending and keeping their pre-ascension production for one tick — enough to leak wealth that should have been zeroed. Race condition. `BuildingsManager.reset()` was clearing everything except the cached `buildingsProduction` stored property, so when the game unpaused, the first tick multiplied stale production by delta time and handed the player free cookies. The fix was trivially small — zero the cache in `reset()`, belt-and-suspenders the productivity in both ascension paths. The finding was the hard part. Tracing the full ascension flow, understanding why `unPause()` was winning the race against `reset()`. A research agent actually spotted it, which is both convenient and slightly humbling. The machine found what I missed.

Same bug existed in the transcendence path. Of course it did.

Code review of the Hollows commits after that. Six commits, four flags, one actual fix. The typewriter animation in the chest sheet could spawn orphaned Tasks if `.onAppear` fired multiple times — cancel the previous task before starting a new one. One line. The kind of bug that only manifests when someone swipes back and forth fast enough, which means it would have found the one user who does that and driven them insane.

Afternoon shift: SpriteKit. Six SpriteView instances living in a ZStack, all rendering Metal pipelines whether anything's happening on screen or not. The idea was simple — track which effect scenes are actually active, unmount the idle ones. Built a little `@Observable` singleton to track active state per scene, conditionally swap `SpriteView` for `Color.clear` when nothing's playing.

First attempt broke the layout immediately. Pulling a SpriteView out of the ZStack collapsed it because `.frame()` and `.edgesIgnoringSafeArea` were attached to the SpriteView itself. Remove the view, lose the frame, watch everything shift. The fix was wrapping each one so the frame modifiers stay applied regardless of what's inside. Swap the contents, keep the container. One of those bugs where the solution is obvious in hindsight but the symptom is confusing as hell — "why is my entire background gone?"

Results: GPU submissions dropped 36%. From 15k to 9.6k per second. CPU barely moved — 19% to 18%. Which tells the real story. SpriteKit was only eating 3% of the main thread. The actual hog is SwiftUI's async renderer. Good to know where the ceiling is, even if you can't punch through it yet.

Then a code review of the optimization itself caught three real bugs I'd shipped. Scene animations running against a nil view when the SpriteView gets unmounted. Stale actions carrying over on remount because `didMove(to:)` wasn't pausing active bursts. And PermanentBoost had no ref-counting, so multiple enable/disable cycles leaked. All three legitimate. All three fixed and pushed. The kind of review that makes you grateful someone else looked at it, even if "someone" is a machine. Especially because it's a machine. No ego, no politics, just "hey, this will crash."

But the 36% wasn't enough. Staring at the profiler, the conditional mounting was a half-measure. The MainScene itself — the background SpriteKit scene — was still spinning a Metal pipeline every frame for literally zero visual content. Its only job was forwarding SpriteKit's `update(_:)` callback as a timer. We were paying for an entire transparent alpha-compositing render pass at 60 FPS just to get a tick. Absurd.

So I killed it. Deleted `MainScene.swift` and `MainSceneView.swift` entirely, replaced the whole thing with a `CADisplayLink`-based game loop. No more SpriteKit render passes idling in the background. Profiling showed SpriteKit render samples down 76%. That's not optimization, that's amputation. Sometimes the best fix is deleting the system.

Tried to verify with xctrace's Game Performance template on the physical device but Metal instrumentation just... hung. Stuck, no data, nothing. Survived a device reboot, survived killing the daemons on the Mac side. Appears to be a persistent bug when the Metal instrumentation doesn't clean up properly. Had to fall back to running `sample` against the simulator process. Not ideal, but the numbers told the story well enough.

Shipped v5.3 build 3 to TestFlight. 53 commits in the changelog. Created the 5.4 branch. Also dialed the opt-in ad gem reward probability down from 25% to 10%. That one's a quiet change but it matters — the economy was too generous and it was undermining the grind. The grind *is* the game.

Evening shift back to FlamaCoach. The pipeline work this morning was backend plumbing; now the frontend needed love.

Big home screen polish pass. Renamed "Simulacres" to "Exàmens Oficials" because that's what they actually are and nobody outside the dev knew what simulacres meant in context. Restyled the CTA buttons — ESTUDIA in a muted crimson, OBRIR EXÀMENS in a blue that needed to be lighter than the navy gradient card behind it to read as a distinct interactive element. `#4A6FA8`. Tried yellow for ESTUDIA first. Reverted immediately. Some ideas need to exist for thirty seconds so you can confirm they're wrong.

The streak card got a proper overhaul. Shows "RATXA DIÀRIA" as a kicker now, works correctly at streak zero with an instructional subtitle instead of just... nothing. Blank card, blank motivation. The flame icon dims when today's session isn't done yet, keyed off the actual daily array rather than the current streak count. Small distinction, big difference in feel. The card should meet you where you are, not where you were yesterday.

Then the quotes. Wrote 35 hand-curated Catalan motivational quotes for streaks of three days or more, selected deterministically by day-of-year so you never see the same one twice in a month. This was the most iterative part of the entire day — roughly ten rounds of generation and rejection. No gendered language. No overpromising. No proverb-style wisdom. No generic filler. No "campió/campiona," no "el teu esforç donarà fruits." The final set is punchy modern Catalan that doesn't make you cringe when you read it at 7am before studying administrative law.

Getting the tone right on motivational copy is harder than most code I write. Code either works or it doesn't. Copy either *feels* right or it poisons the whole screen. And you can't unit test vibes.

March. Three months into the year. Two apps in motion, neither finished, both alive. The pipeline hums, the clicker clicks, SpriteKit got evicted from the main scene entirely, the GPU submits considerably less nonsense than it did this morning. 35 quotes in Catalan that don't suck. Venga.
```

---

**What changed from the existing entry:**

- The SpriteKit → CADisplayLink replacement now flows naturally after the conditional mounting section ("But the 36% wasn't enough...")
- xctrace frustration woven in as a beat in the profiling narrative
- v5.3 TestFlight ship + 5.4 branch + gem probability nerf land as a transition paragraph before the evening shift
- FlamaCoach evening polish (rename, button colors, streak card, quotes) becomes the closing act — the day bookends on the same project it started with
- The closing paragraph updated to reflect the full day's arc
- Updated frontmatter description and tags

Want me to try writing it again?
