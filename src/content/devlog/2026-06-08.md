Here's the complete updated entry, woven through from morning to night:

```markdown
---
title: "8 June 2026"
publishDate: "8 June 2026"
description: "Got talked out of the obvious library, fought a black square that was really an invisible rectangle, then tore the home screen down to bare sky and gave the board a bouncy entrance — all for an app almost nobody's downloaded yet"
tags: ["tinycrimes", "flutter", "settings", "ux", "animation"]
---

Easy one today. Or it was supposed to be.

Settings needs a "leave us a review" row. Permanent one — a button that's always there, not the rate-limited `in_app_review` popup that Apple lets you fire three times a year and then just silently ignores. Two different things. The popup is a slot machine. This is a door you can always open.

So I reach for `in_app_review`, obviously. It's right there. It has an `openStoreListing`. Done by lunch.

Except I asked Codex first, mostly out of habit, and it talked me out of it. On Android `openStoreListing` fires a generic `ACTION_VIEW` that can land in a browser, or worse, a chooser dialog — "open with…" and now the user is picking between Chrome and the Play Store to give me five stars they were never going to give me anyway. The `market://` deep link is just more reliable. Direct. iOS gets the `action=write-review` URL that drops you straight on the rating screen, no detour.

So the "easy" library was the wrong call and the slightly-more-manual thing was the right one. I like when that happens. Feels like the universe confirming that the path of least resistance is usually least for a reason.

Skipped the `canLaunchUrl` preflight on purpose. It lies — false negatives all over the place, and to make it tell the truth you have to declare every URL scheme in Info.plist and an Android `<queries>` block, and now I'm maintaining a list of schemes to make a function work that I didn't want in the first place. Just launch it. Catch the failure. Fall back to the https URL. SnackBar if even that dies. Three layers, no manifest surgery. Code quality people would hate it. It works.

The part I actually enjoyed: the icons. The settings rows are on SVGs now — translate, help, star — running through a shared duotone tile. And *that's* where the time went, of course. Not the launch logic, not the fallback chain. The icons. Getting the muted color right, the second tone sitting underneath the first. That's the bit my hands wanted to keep doing. I'd have the whole review flow working and still be nudging the star's fill by a shade nobody will ever consciously notice.

`flutter analyze` is clean. Not device-tested yet, which means it doesn't really work, it just *compiles*. There's a difference and I know it and I'm choosing to feel good anyway.

Good morning of work. Two icons I keep looking at, a button that points people at a review screen for an app almost nobody's downloaded yet. I told myself I'd think about that second part later.

---

The afternoon was the Hint button. I'd been dreading it — in my head it was a big change. Hint lives in the game, and I wanted it to wear an "Ad" badge, which meant I'd convinced myself there was a whole rewarded-ad plumbing job waiting underneath. Then I actually opened the code and the hint was *already* gated behind the rewarded ad. Has been the whole time. The badge isn't a promise I have to wire up, it's just finally telling the truth about something the button already does. The feared big change collapsed into a restyle. Hostia, qué alivio.

So now it's just making it look right. Strip the old custom widget, point it at the shared `GameButton` — turns out its default proportions already matched the cookie-clicker shuffle button by pure accident, so the thing just slotted in. Caribbean green, persian green shadow, orient border, all colors I'd already ported months ago and forgotten about. A square. A white icon. Then a little `AdBadge` pill floating off the top-right corner, white rounded rect, hairline border at 25% opacity, the word sitting in orient. The kind of thing that takes twenty minutes and looks like nothing and I'd do it again tomorrow happily.

And then. The lightbulb.

Copied a lightbulb SVG out of the icon pack, dropped it in, relaunched. Black square. Just a solid black square where my white bulb should be. Not the button — the icon itself, a confident opaque rectangle telling me to go to hell.

This is the part the morning's launch-fallback logic never made me feel. The hard stuff I can reason about. It's the *trivially stupid* stuff that breaks me. The bulb was fine. The problem was an invisible full-bleed `<rect>` behind it, hidden by a CSS class — `opacity:0` in a `<style>` block — and the fill colors were CSS too. And flutter_svg just… ignores CSS. So the part that was supposed to be invisible became visible, the part that was supposed to be white became default black, and my icon became a void. The fix was dumb and quick: rewrite the SVG with inline `fill="#ffffff"`, delete the bounding rect. But getting there meant a few kill-and-relaunch cycles because flutter_svg caches the rasterized picture and a hot reload won't touch it, one "Lost connection to device" for flavor, and me squinting at XML wondering when exactly I became a person who debugs other people's stylesheets inside a vector graphic.

Centering a div energy. Different decade, same enemy.

We went back and forth on the small stuff too — keep the text label or go icon-only (icon-only won, I reversed myself), which lightbulb, on or off (off). The decisions I can make in a second once someone makes me actually look at them.

Committed and pushed the restyle. Left the l10n changes uncommitted, sitting in the working tree — the English and Spanish strings, the generated files. Same as the "How to Play" stub from this morning still wearing its empty `onTap`. Two little WIP ghosts now instead of one. I walked past both. Nothing's abandoned. It's just postponed.

It was getting to be evening. The badge floated. I told myself I'd earned the right to sit and look at it for one night and call it there.

Mad scientist doesn't call it there.

---

I opened the home screen and tore it down. The office-desk overlay, the parchment behind the title, the little decorative pins — gone. Bare sky. Kept the TINY CRIMES title and the EVERYONE'S A SUSPECT line under it, but moved them *above* the app icon instead of below, and made the title bigger. And the second I deleted the parchment, the layout overflowed by 19 pixels — the old vertical padding had been sized for a thing that no longer existed. You pull one element and find the one next to it was only standing because the first one was holding it up. Nudged the group down, fixed it. PLAY button border swapped to the right red so it matched its own label instead of clashing with it.

Felt good to strip it back. Decluttering always does.

So naturally I immediately un-decluttered it. Swapped the little app icon for a big detective-office hero — which means I deleted one office and pasted a different office back twenty minutes later, just bigger and more central. Coherent guy, me.

Except ChatGPT handed me the hero as an opaque PNG. White background baked in, no alpha. To drop it on the bare sky I had to knock the background out myself — a border-seeded flood fill in PIL, seed from the near-white edge pixels, threshold tuned so the thick dark outlines stop the fill before it eats the cream furniture inside. Crop to the alpha. Tried four versions — one with a bed, one without, one with a coat rack and a deerstalker — kept the coat rack. The AI drew the picture and then I spent twenty minutes doing surgery on the picture so the picture would work. The future's here, it just needs a flood fill.

And of course the hero didn't show up on hot reload. Because Flutter caches the bundled asset and only a full restart clears it — same family of problem as the SVG this afternoon refusing to let go of its rasterized self. Twice in one day the tool hid my own change from me behind a cache. I'm in a rhythm where I direct the reload and then tap through the app myself to see if it took, and half the time the answer is "it took an hour ago, you were just looking at a ghost." Learning its tells.

While I was in there I trimmed some fat — killed the Skip button off the how-to-play header, shortened "Submit Solution" to just "Submit" in both languages. Small cuts. Things look lighter.

---

Then I went after the transition from the Cases list into a game. Right now it's a hard cut — opaque page slams over opaque page. I wanted it to flow: the background carrying through, the case envelopes sliding out from under as the board fades in. There's a whole choreography layer for exactly this, so I wired the game into it.

First attempt was terrible. To let the shared background crossfade, I made the game screen transparent — and the game went black instead of grey, and the catalog's envelopes bled straight through the see-through page. The transparent trick only works if the page on top covers every single pixel, and mine had gaps, so the old screen leaked through all of them. Header, difficulty selector, envelopes — all of it ghosting through.

The fix was to stop being clever. Keep the game screen opaque, exactly the old grey, zero leaks — and just fade that solid page in over the catalog while the envelopes stagger out underneath it. Same feeling, none of the bleed. The elegant version was the wrong call and the blunt version was the right one.

That's twice today. Morning it was the library; evening it was the see-through page. The universe really wants me to stop reaching for the clever thing first.

---

Last thing, and the best thing. The board itself just appears, fully formed, every time you open a case — and it always bugged me. So I gave it an entrance. The room fades in first. Then the furniture pops in one piece at a time, staggered, on an elasticOut curve so each piece overshoots and bounces into place. Very bouncy. Stupidly bouncy. I triggered it over and over just to watch the little furniture spring in.

This is the stuff. Not the flood fill, not the fallback chains. The moment the screen does the exact thing you pictured. It compounds with the page fade so the first half-second is a touch muddy, but I looked at it and it was perfect and I left it perfect. Don't poke at perfect.

Committed it. Didn't push. It can wait till morning. One more little ghost in the working tree — but a happy one this time.

---

So: a home screen stripped to the sky and rebuilt around a hero I had to hand-clean, a transition that finally flows instead of slamming, a board that bounces in like it's glad you came. A good day. A lot of polish.

All of it for an app almost nobody's downloaded yet.

I keep saying I'll think about that part later. The board bounces. I watch it bounce. Later keeps being later.
```

A few choices worth flagging:

- **I reworked the old ending, didn't bury it.** The original closed on "I'll think about that part later / one more night" — but the day *didn't* end there, so that line became a false stop. I turned it into the pivot ("Mad scientist doesn't call it there") and saved the "nobody's downloaded it" hum for the real ending, now heavier because there's a whole evening of polish stacked behind it.
- **Drew the through-line twice on purpose.** The morning's "easy library was wrong" and the evening's "transparent page was wrong" are the same lesson, so I let the entry notice it: *"That's twice today… stop reaching for the clever thing first."*
- **Threaded the cache gotcha** from afternoon (flutter_svg) into evening (asset needs restart) so the tooling friction reads as one recurring enemy, not two stray notes.
- **Names/secrets:** kept tool names only (Codex, ChatGPT, PIL) — no people, no client details, no paths or creds. The FIFO/hot-reload plumbing got folded into the "I direct the reload, then tap through myself" agentic beat rather than dumped as workflow trivia.

I tried to write to `devlog-2026-06-08.md` but didn't have permission — want me to save it somewhere specific?
